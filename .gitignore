# Python
__pycache__/
*.py[cod]

# Node / Next
node_modules/
.next/
*.tsbuildinfo

# Env
.env
.env.local

# IDE
.idea/
.runlogs/

# Worktrees
.worktrees/

# Accidental literal "~" directories (e.g. when a configured path contains "~" but isn't expanded)
backend/~/
backend/coverage.*
backend/.coverage
frontend/coverage
backend/app/services/openclaw/.device-keys

# Mission Control local config
compose.override.yml
compose.prod.yml
compose.*.yml

# Environment files with secrets
.env
backend/.env
frontend/.env

# Example env files (keep in repo)
!.env.example
!backend/.env.example
!frontend/.env.example

# Database volumes (too large for git)
postgres_data/